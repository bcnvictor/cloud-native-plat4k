# Database
POSTGRES_USER=cnpuser
POSTGRES_PASSWORD=cnppass
POSTGRES_DB=cnp

# Backend
# SECRET_KEY MUST be at least 32 characters long for Fernet encryption
SECRET_KEY=change_this_to_a_secure_random_string_of_at_least_32_characters
ENCRYPTION_KEY=  # Clé Fernet dédiée au chiffrement des credentials (distinct du SECRET_KEY JWT). Générer avec : python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECURE_COOKIES=false  # Passer à true en production (HTTPS obligatoire)
LOG_LEVEL=INFO

# Kubernetes
KUBECONFIG_PATH=~/.kube/config  # Chemin vers le kubeconfig (en local). En prod AKS, laisser vide (in-cluster config).
K8S_TARGET_NAMESPACE=default    # Namespace cible pour les déploiements applicatifs
K8S_IMAGE_PULL_SECRET=          # Nom du secret Docker dans le namespace cible (ex: epita-registry)

# GitLab CI bot (injection de pipeline)
# Dette technique : PAT personnel utilisé faute de Group Access Token sur gitlab.cri.epita.fr
# À remplacer par un Group Access Token lors de la migration GitLab
GITLAB_BOT_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
GITLAB_BOT_NAMESPACE= # Namespace hébergeant cnp-ci-templates
CNP_API_BASE_URL=http://localhost:8000  # URL publique de l'API CNP (doit être accessible depuis les runners GitLab)
GITLAB_WEBHOOK_SECRET=  # Secret partagé pour vérifier les webhooks GitLab entrants (X-Gitlab-Token)

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cnp-backend"
version = "0.1.0"
description = "Cloud Native Platform Backend"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "asyncpg",
    "alembic",
    "python-jose[cryptography]",
    "passlib==1.7.4",
    "bcrypt==3.2.2",
    "pydantic-settings",
    "pydantic[email]",
    "cryptography",
    "psycopg2-binary",
    "slowapi",
    "kubernetes",
    "httpx",
    "python-gitlab",
    "python-multipart",
    "pyyaml",   # _validate_kubeconfig dans cluster_service
    "hvac",
    "anyio>=4.0",  # anyio.move_on_after + anyio.to_thread dans cluster_service
]

[tool.setuptools]
package-dir = {"backend" = "."}
packages = ["backend", "backend.api", "backend.core", "backend.db", "backend.k8s", "backend.gitlab", "backend.services", "backend.api.schemas", "backend.vault"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]   # pycodestyle errors, pyflakes, isort
ignore = ["E501", "E712"]   # E501: line length géré par le formateur
                             # E712: SQLAlchemy requiert `== False/True` dans les filtres ORM

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-asyncio",
    "aiosqlite",
    "httpx",
]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
